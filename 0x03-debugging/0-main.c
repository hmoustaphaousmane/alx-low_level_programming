#include "main.h"

/**
 * main - Entry point of the program (test the function
 * positive_or_negative())
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
